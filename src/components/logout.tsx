import { Container, Typography } from "@mui/material";

const Logout = () => {

  return (
    <Container>
      <Typography variant="body1">{'Вы успешно вышли из приложения.'}</Typography>
    </Container>

  );
}

export default Logout;